<div class="col-md-12 mt-2 text-right">
  <%= form_with url: search_posts_path, local: true, method: :get do |f| %>
    <div class="field">
      <%= f.text_field :keyword %><%= f.submit "検索", class: "search-button" %>
    </div>
  <% end %>
</div>